org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Native loco-server / microinit / valkey for "BigFred on phone" (GitHub tip or Releases).
# Prefer local ../bigfred/bin/loco-server-android-arm64, then native-prebuilt/,
# then `make loco-android` / `make microinit-android` / `make valkey-android`
# (see Makefile BIGFRED_REF / MICROINIT_REF / VALKEY_REPO).
