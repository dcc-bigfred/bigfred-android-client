org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Native loco-server / microinit for "BigFred on phone" (GitHub Actions tip or Releases).
# Prefer local ../bigfred/bin/loco-server-android-arm64, then native-prebuilt/,
# then `make loco-android` / `make microinit-android` (see Makefile BIGFRED_REF / MICROINIT_REF).

# Valkey Android prebuilt (latest GitHub release).
depsAndroidValkeyRepo=dcc-bigfred/deps-android-valkey
